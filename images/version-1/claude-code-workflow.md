---
path_id: claude-code-getting-started (stage 3 focus)
title: Claude Code — The Daily Development Workflow
concept: The core Explore → Plan → Code → Commit cycle — the agentic loop in practice
---

## Image Generation Prompt

A circular workflow diagram rendered as a polished product illustration. The composition is centered, symmetrical, and built around four quadrants of a glowing ring. Background: deep navy #0D1B2A.

**The Ring:**
A large circular track divided into four equal arcs, each a different color. The ring is a bold rounded path with a soft metallic sheen and inner glow. Smooth curved arrows between each quadrant indicate clockwise flow.

**Quadrant 1 — EXPLORE (top arc, teal #00BCD4):**
Inside the quadrant: a magnifying glass scanning a branching file tree. Floating micro-icons: grep search pattern, file navigation breadcrumb, dependency graph nodes. A Claude response bubble shows: *"Here's what I found in the codebase…"*

**Quadrant 2 — PLAN (right arc, amber #F59E0B):**
Inside: a structured markdown checklist with checkmarks appearing line by line. A partial Claude Code plan response is visible — numbered steps, scope notes, risk callouts. A calendar and time-estimate icon float nearby.

**Quadrant 3 — CODE (bottom arc, violet #7C3AED):**
Inside: a terminal window streaming green-on-dark output — lines of code appearing, diffs highlighted (green adds, red removes). An approval mode badge reads: *"Ask Mode — awaiting approval."* A progress bar tracks file writes.

**Quadrant 4 — COMMIT (left arc, emerald #10B981):**
Inside: a git branch diagram — main branch with a feature branch, a tagged commit node, a PR icon, and a merge arrow. Green checkmark on a CI/CD pipeline. A GitHub badge confirms: *"✓ Tests passed."*

**Center Hub:**
The Claude shell-wave logo mark glows at the center where all four arcs meet. A soft radial gradient emanates outward like a heartbeat. Text below the logo: *"Agentic Loop."*

**Connecting arrows:** Smooth gradient-colored curved arrows between each quadrant, clockwise. Each arrow blends the two adjacent colors it connects.

**Floating context in the corners:**
- VS Code window icon (top-left, small)
- JetBrains IDE icon (top-right, small)
- Terminal window icon (bottom-left, small)
- Claude Desktop badge (bottom-right, small)

**Style:** Modern product illustration. Clean, bold, geometric. Flat design with glow effects and subtle depth. No textures. No photorealism.

**Palette:** Navy #0D1B2A · Teal #00BCD4 · Amber #F59E0B · Violet #7C3AED · Emerald #10B981 · White #F8FAFC

**Format:** 1:1 square or 4:3. Suitable for an infographic card, social media, or course section divider.

**Mood:** Clear, rhythmic, productive. A workflow you return to every single day.
