---
path_id: api-developer
title: Building with the Claude API
level: intermediate
audience: software engineers, ML engineers, AI application developers, backend developers, data scientists building AI systems
core_concepts: API authentication, prompt engineering, RAG, tool use, MCP integration, Claude Code integration, Computer Use, autonomous agent architectures
total_hours_required: 8.1h (84 lectures)
---

## Image Generation Prompt

A detailed technical illustration of a developer building AI-powered applications with the Claude API. Composition: a vertical stack of five layers showing the full curriculum from first API call to production agent system. Dark engineering aesthetic. Background: deep graphite #111827.

**Layer 1 — API Foundation (bottom, orange #F97316):**
An API call diagram: developer laptop → HTTPS arrow → Anthropic API endpoint (cloud icon with authentication shield). A JSON response object floats nearby, partially visible. Label: *Authentication & Basic API Calls*.

**Layer 2 — RAG · Knowledge Retrieval (lower-middle, blue #3B82F6):**
A flowing pipeline diagram:
Document icons → Embedding model (transformation arrows) → Vector store (dot grid with similarity markers) → Claude query → Retrieved context bubble → Synthesized response.
Color-coded in blue. Label: *Retrieval-Augmented Generation*.

**Layer 3 — Tool Use (middle, violet #8B5CF6):**
Claude's reasoning loop with the function-call protocol visible:
Claude → *"I'll call search()"* → tool execution (spinning gear) → result returned → Claude incorporates → final answer.
Tool icons floating around: calculator, web search, code executor, database query. Label: *Tool Use & Function Calling*.

**Layer 4 — Agent Architectures (upper-middle, teal #14B8A6):**
A multi-agent orchestration diagram: Orchestrator Claude at top dispatches to three Worker Agents below (each with their own context window icon). Arrows show task delegation and result collection. A Computer Use panel shows a cursor operating a web browser. Label: *Agent Architectures & Computer Use*.

**Layer 5 — MCP Integration (top, emerald #10B981):**
A compact MCP ring showing Claude connected via MCP to four external resources: database, REST API, file system, GitHub. Label: *MCP · External System Integration*.

**Connecting elements:**
Vertical arrows between layers show how each concept builds on the previous. A developer figure stands to the right of the stack, gesturing toward it — the architect of this system.

**Course scale badge (top-right corner):**
*"84 lectures · 8+ hours · Full API spectrum"*

**Style:** Technical stack diagram elevated to product illustration. Dark engineering aesthetic. Each layer has its own color family with subtle horizontal separators. Clean consistent icons throughout. No photorealism.

**Palette:** Graphite #111827 · Orange #F97316 · Blue #3B82F6 · Violet #8B5CF6 · Teal #14B8A6 · Emerald #10B981 · White #F9FAFB

**Format:** 9:16 portrait (tall stack) or 16:9 with layers arranged side by side.

**Mood:** Comprehensive, technical, ambitious. This is the full-spectrum path — from hello world to production agents.
